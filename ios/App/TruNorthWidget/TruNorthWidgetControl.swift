// Intentionally empty.
//
// Xcode's Widget Extension template generates a Control-Center "timer" widget
// here. TruNorth ships only the Home/Lock-Screen "Your Basket" widget (see
// TruNorthWidget.swift), so this file is left empty on purpose. Deleting it in
// Xcode is fine too — it's kept as an empty stub so the target's build-phase
// reference stays valid without editing the project file by hand.
